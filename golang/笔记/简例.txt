package main
import "fmt"
func main (){
	fmt.Println("hello,world!")
}
//go文件的后缀是.go
//package main 
//表示该文件所在包是main，在go中，每个文件都必须归属于一个包。
//import "fmt"
//表示 ：引入一个包，包名是fmt，引入该包后，就可以使用fmt包函数，比如：fmt.Println
//func是一个关键字 表示函数，
//main是函数名，是一个主函数，即我们程序的入口
//fmt.Println("hello")
//表示调用fmt包的函数 Println表示输出；
/*    块注释    */
